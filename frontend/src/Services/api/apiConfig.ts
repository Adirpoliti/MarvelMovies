import { BASE_URL } from "./baseURL";

export const apiConfig = {
    apiNewMovie: BASE_URL + "/api/movies/new",
    apiAllMovies: BASE_URL + "/api/movies/all",
    apiSearchedMovies: BASE_URL + "/api/movies/search/"
}